#!/usr/bin/env tsx

console.error(
  'scripts/start-production.ts is retired. Run the protected release plan and separately approved release apply before deployment, then start the application with pnpm start:prod.',
);
process.exit(1);
