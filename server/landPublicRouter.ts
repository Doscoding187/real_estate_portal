import { z } from 'zod'; import { publicProcedure, router } from './_core/trpc'; import { publicLandDetail, searchPublicLand } from './services/landPublicService';
export const landPublicRouter = router({ search: publicProcedure.input(z.object({ classification: z.enum(['residential_stand','development_land','commercial_industrial_land']).optional(), city: z.string().optional(), province: z.string().optional(), minPrice: z.number().positive().optional(), maxPrice: z.number().positive().optional(), minSize: z.number().positive().optional(), maxSize: z.number().positive().optional() }).optional()).query(({ input }) => searchPublicLand(input)), detail: publicProcedure.input(z.object({ slug: z.string().min(1) })).query(({ input }) => publicLandDetail(input.slug)) });
