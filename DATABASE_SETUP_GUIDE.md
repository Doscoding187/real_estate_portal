# Retired Database Setup Guide

> **Retired.** This historical guide is not operational database authority.
> Its Docker, manual SQL, seed, and direct database setup instructions must
> not be used.

Current database operations are governed by the
[Database Authority entry contract](docs/database-authority/00-database-authority-agent-entry.md),
[Database Change Protocol](docs/database-authority/database-change-protocol.md),
and [canonical migration README](server/migrations/README.md).

Production seed authority is `none`. Local and test database lifecycle work
must use only the approved guarded workflows. Historical content remains
available through Git history.
